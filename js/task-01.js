const navCtgrs = document.querySelectorAll(".item");
console.log("Number of categories:", navCtgrs.length);

const navEl = document.querySelectorAll("h2");
console.log(navEl.length);

console.log("Category:", navEl[0].textContent);
